# Rust build output
/target
# Tauri-generated schemas/bindings and mobile projects (regenerated by
# `tauri ios init` / `tauri android init`). Commit gen/apple yourself only if
# you want the Xcode signing settings under version control.
/gen
